node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Uploaded documents (Aadhar/PAN/residence proof) live outside the repo (see UPLOAD_DIR in .env)
uploads/
